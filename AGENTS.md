# CodexLab Rules

- Work only inside `/home/usow/codexlab`.
- Do not edit, move, or delete files outside `/home/usow/codexlab`.
- Read-only reference access is allowed for `/home/usow/.codex/memories/**` and `/home/usow/.codex/skills/**` when it materially helps the task.
- Built-in tools such as shell execution and JS REPL may be used when needed to complete the task.
- Runtime state lives under `control/`, `tasks/`, and `agents/*/{home,workspace}`.
- Treat `.codex-home/` as local runtime state, not product source.
- If outside write access is required, stop and ask.
